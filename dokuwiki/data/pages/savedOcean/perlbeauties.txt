* Functional programming rulez:
    my $ret =
        '(' .
        (join ', ',
         map {join ', ', @$_}
         map {[join(' ^ ', map {"*$_"} @$_),
               fold {$_[0]*$self->varSize($_[1])} 1, @$_]}
         map {[split /\W+/]}
         map {$term->{$_}}
         qw/searched known/) . ')';
